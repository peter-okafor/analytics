import React from 'react';
import { MainSearch } from '../components/secondary';

export default {
  title: 'Analytics/MainSearch',
  component: MainSearch
};

const Template = (args) => <MainSearch {...args} />;

export const Input = Template.bind({});
